# This app intentionally does NOT store user API keys server-side.
# Keys are provided by the user in the browser and stored in sessionStorage.

# Optional defaults (UI still supports manual entry)
NEXT_PUBLIC_MISTRAL_API_KEY=
NEXT_PUBLIC_ELEVENLABS_API_KEY=

# Optional: default voice id for ElevenLabs usage (not wired yet)
# NEXT_PUBLIC_ELEVENLABS_VOICE_ID=
